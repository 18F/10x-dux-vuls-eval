---
    applications:
    - name: ((app_name))-edb-((env))
      instances: ((default_instances))
      memory: ((default_memory))
      disk: ((default_disk))
      command: go-exploitdb server --bind=0.0.0.0 --port=8080 --dbtype postgres --dbpath "$DATABASE_URL" --debug-sql
      docker:
        image: ohsh6o/vuls-combined:latest
      services:
      - ((rds_instance))
      routes:
      -  route: ((app_name))-edb-((env)).apps.internal

    - name: ((app_name))-cvedb-((env))
      instances: ((default_instances))
      memory: ((default_memory))
      disk: ((default_disk))
      command: go-cve-dictionary server --bind=0.0.0.0 --port=8080 --dbtype postgres --dbpath "$DATABASE_URL" --debug-sql
      docker:
        image: ohsh6o/vuls-combined:latest
      services:
      - ((rds_instance))
      routes:
      -  route: ((app_name))-cvedb-((env)).apps.internal

    - name: ((app_name))-ovaldb-((env))
      instances: ((default_instances))
      memory: ((default_memory))
      disk: ((default_disk))
      command: goval-dictionary server --bind=0.0.0.0 --port=8080 --dbtype postgres --dbpath "$DATABASE_URL" --debug-sql
      docker:
        image: ohsh6o/vuls-combined:latest
      instances: ((default_instances))
      services:
      - ((rds_instance))
      routes:
      -  route: ((app_name))-ovaldb-((env)).apps.internal

    - name: ((app_name))-gost-((env))
      instances: ((default_instances))
      memory: ((default_memory))
      disk: ((default_disk))
      command: gost server server --bind=0.0.0.0 --port=8080 --dbtype postgres --dbpath "$DATABASE_URL" --debug-sql
      docker:
        image: ohsh6o/vuls-combined:latest
      services:
      - ((rds_instance))
      routes:
      -  route: ((app_name))-gost-((env)).apps.internal

    - name: ((app_name))-msfdb-((env))
      instances: ((default_instances))
      memory: ((default_memory))
      disk: ((default_disk))
      command: go-msfdb server server --bind=0.0.0.0 --port=8080 --dbtype postgres --dbpath "$DATABASE_URL" --debug-sql
      docker:
        image: ohsh6o/vuls-combined:latest
      services:
      - ((rds_instance))
      routes:
      -  route: ((app_name))-msfdb-((env)).apps.internal

    # - name: ((app_name))-core-((env))
    #   memory: 128m
    #   command: server -debug -debug-sql -to-localfile -format-json -log-dir=/var/log -results-dir=/vuls/results -listen=0.0.0.0:5515 -cvedb-type=http -cvedb-url=http://cvedb:1323 -ovaldb-type=http -ovaldb-url=http://ovaldb:1324 -gostdb-type=http -gostdb-url=http://gostdb:1325 -exploitdb-type=http -exploitdb-url=http://exploitdb:1326
    #   docker: 
    #     image: ohsh6o/vuls-combined:latest
